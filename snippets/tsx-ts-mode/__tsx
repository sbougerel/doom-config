# -*- mode: snippet -*-
# name:
# uuid:
# key: __tsx
# condition: t
# --
export default function `(user/pascal-case (file-name-base buffer-file-name))`() {
  ${0}

  return (
    <div className="">
    </div>
  );
}
